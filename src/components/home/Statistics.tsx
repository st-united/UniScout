// import { useTranslation } from 'react-i18next'; // Remove unused import
// import { mockStatistics } from '../../data/mockData'; // Remove unused import

const Statistics = () => {
  // const { t } = useTranslation(); // Remove unused hook result

  return (
    <div>
      <h2>Statistics Section</h2>
    </div>
  );
};

export default Statistics;